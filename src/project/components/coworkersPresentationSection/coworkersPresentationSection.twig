{#
 * @title: "coworkersPresentationSection" TWIG view component
 * @description:
 #}

<div class="coworkersPresentationSection {{ classElement }}">
    <section class="coworkersPresentationSection_wrapper">
        <div class="coworkersPresentationSection_content">
            <h1 class="coworkersPresentationSection_title">{{ dico.coworkersPresentationSection.title }}</h1>

            <div class="coworkersPresentationSection_text">
                {# TODO text en ADMIN depuis la page "home" #}
                {% for paragraph in dico.coworkersPresentationSection.paragraphs %}
                    <p>{{ paragraph }}</p>
                {% endfor %}
            </div>
        </div>
    </section>
</div>
